/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', '127.0.0.1', 'res.cloudinary.com', 'https://res.cloudinary.com', 'https://api.biziffy.com'],
    }
};

export default nextConfig;
